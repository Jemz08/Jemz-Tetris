# Fonts

The game uses two free pixel-style fonts loaded from Google Fonts:

- **Press Start 2P** — retro blocky headings and buttons
- **VT323** — readable terminal-style body text

Both are OFL-licensed and free for commercial use. If the CDN is unreachable,
the stylesheet falls back to `Courier New` / `monospace`, so the game still works.
